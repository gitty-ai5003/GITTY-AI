# API Specifications

Contains details of the exposed endpoints:

- **API Gateway (FastAPI)**: Served at `http://localhost:8000`. Swagger available at `/docs`.
  - `/api/v1/health`: Gateway unified health checking.
- **Auth Service (Express)**: Served at `http://localhost:3000`.
  - `/health`: Local health check.
